class Solution {
public:
    bool isPalindrome(int x) {
        if (x < 0) return false;
        int n = x;
        int rev = 0;
        while(x>0){
            int ld = x % 10;
            if (rev > (INT_MAX - ld) / 10) {
                return false; // Would overflow if we proceed
            }
            rev = (rev * 10) + ld;
            x = x/10;
        }
        return (n == rev);
    }
};
